// The businesses in here are real and findable; the quotes are not. Nobody has
// actually vouched yet, so this says so plainly rather than letting invented
// words look like real statements from real shops.
export default function PrototypeNotice() {
  return (
    <div className="border-b border-rule bg-paper-2 px-8 py-2.5">
      <p className="text-[10px] uppercase tracking-label text-muted-2">
        Prototype — shops and locations are real research data. All quotes and
        vouches are sample content, not real statements.
      </p>
    </div>
  );
}
